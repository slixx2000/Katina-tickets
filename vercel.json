{
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api/[...all]"
    }
  ],
  "functions": {
    "api/[...all].ts": {
      "includeFiles": "{server/**,shared/**,prisma/**}"
    }
  }
}
